syntax = "proto3";

package brizyMessage;

message Message {

  enum DescriminatorType {
    NONE = 0;
    DOCTRINE_MESSAGE = 1;
    TRANSLATE_ENTITY_MESSAGE = 2;
  }


  DescriminatorType descriminator = 1;
}